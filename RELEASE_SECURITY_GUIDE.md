# 배포본 보안 사용법

이 기능은 개인 배포용의 가벼운 보호 장치입니다. 개발 원본과 설정, 배포본을 Git에서 함께 관리하고, 배포본에는 VBA 프로젝트 잠금과 사용 만료일을 적용합니다.

## 파일 구분

- `config.json`: 평문 비밀번호를 포함한 실제 설정. 개인 저장소의 Git에 포함됩니다.
- `config.example.json`: 비밀번호를 제외한 설정 예시입니다.
- `workbooks/dev/*.xlsm`: 개발 원본. Git에 포함됩니다.
- `dist/`: 생성된 배포본. Git에 포함됩니다.
- `vba-files/`: Codex가 수정하는 VBA 소스. Git에 포함됩니다.

## 새 버전 시작 및 소스 변경 후

새 버전 개발을 시작하면 Git의 최신 보안 VBA 소스가 새 개발 원본에 자동 반영됩니다. 개발 도중 VBA 소스를 추가로 수정했다면 아래 명령으로 현재 개발본에 다시 반영합니다.

```powershell
npm run release:sync-dev
```

개발 원본에는 배포 보안 표시자가 없으므로 기존처럼 바로 사용할 수 있습니다.

`개발본 보안 VBA 동기화`는 `vba-files`의 보안 모듈과 통합문서 이벤트를 개발용 `.xlsm` 내부에 주입하는 작업입니다. 별도의 새 버전을 만들지 않으며, 현재 개발 브랜치에서 VBA 소스를 수정한 뒤 다시 반영할 때 사용합니다. main에서는 먼저 새 버전 개발을 시작해야 합니다.

## 통합 워크플로 메뉴

아래 명령 하나에서 보안 설정과 모든 배포 작업을 실행할 수 있습니다.

```powershell
npm run workflow
```

주 메뉴에서 `[7] 배포 보안 관리`를 선택하면 다음 기능을 사용할 수 있습니다.

1. 현재 보안 설정 확인
2. 사용 기간, 연장 기간, 프로젝트 암호, 연장 비밀키, 배포 폴더 설정 및 자동 릴리즈
3. 배포 대상 사용자와 이번 배포본의 사용·연장 기간을 지정하여 현재 버전의 배포본 생성

개발본 VBA 동기화는 새 버전 개발 시작 시 자동 실행됩니다. 자동 검증과 연장코드 생성 등 보조 기능은 필요한 경우 `release:validate`, `release:code` 같은 직접 npm 명령으로 실행할 수 있습니다.

개발 원본 경로와 파일 버전은 보안 설정 화면에서 직접 수정하지 않습니다. 새 버전 개발을 시작할 때 워크플로가 `develop/vA.B.C` 브랜치 버전에 맞춰 개발본 파일명과 `config.json`을 함께 변경합니다.

## 변경 작업과 patch 버전

- 설정 확인, 자동 검증, 연장코드 생성은 읽기 전용이며 버전을 변경하지 않습니다.
- 보안 설정 수정 화면에서 실제 값을 바꾸지 않으면 버전이나 파일을 변경하지 않습니다.
- 보안 설정 수정과 개발본 동기화는 개발 원본 변경 작업입니다.
- 배포본 생성은 현재 버전과 설정을 사용해 배포 파일만 만들며 patch 버전을 시작하거나 자동 릴리즈하지 않습니다.
- 보안 설정을 main에서 수정하면 최신 태그의 다음 patch 개발 브랜치를 자동으로 만들고 원격에 게시합니다.
- 개발본 동기화는 새 버전 개발 시작 시 자동 수행되며, 단독 실행은 현재 개발 브랜치에서만 허용됩니다.
- 이미 개발 브랜치 또는 그 하위 기능 브랜치라면 현재 개발 버전에 적용하며 patch를 다시 올리지 않습니다.
- 릴리즈 브랜치에서는 새 보안 변경을 시작할 수 없습니다.
- 보안 설정을 실제로 수정한 경우에는 프로젝트 워크플로가 변경 사항을 자동 스테이징·커밋하고, 기능 브랜치의 부모 병합부터 `develop → release → main` 병합, 배포본 생성·검증, 태그 게시까지 정식 릴리즈를 연속 수행합니다.
- 따라서 보안 설정 수정 뒤에는 VS Code에서 별도로 커밋하거나 `npm run version:release`를 다시 실행할 필요가 없습니다.
- 개발본 동기화, 배포본 생성, 일괄 실행은 자동 릴리즈하지 않고 선택한 작업만 수행합니다.

자동 검증은 원본 배포본을 복사한 임시 파일에서 저장 시나리오를 실행하며, 원본 배포본은 변경하지 않습니다.

`npm run release:config`로 보안 설정을 실제로 수정하면 main에서 다음 patch 개발을 시작하고 작업 성공 후 릴리즈까지 완료합니다. `release:build`는 현재 버전의 배포본만 생성합니다. `release:sync-dev`와 동기화를 포함한 `release:all`은 현재 개발 브랜치에서만 실행되며 자동 릴리즈하지 않습니다. `release:status`, `release:code`, `release:validate`는 읽기 전용입니다.

## 배포본 만들기

```powershell
npm run release:build
npm run release:validate
```

배포본 생성 시 배포 대상 사용자를 반드시 입력합니다. 사용 기간과 1회 연장 기간은 빈 값으로 두면 `config.json`의 `usage_days`, `renewal_days` 기본값을 사용하고, 값을 입력하면 해당 배포본에만 적용합니다. 배포 대상 사용자는 숨김 보안 정보와 암호로 보호된 `배포정보` 시트에 기록되며 파일명에도 포함됩니다. 일반 사용자에게 표시되는 `사용안내` 시트에는 사용자 이름을 표시하지 않습니다.

배포본은 개발 원본을 먼저 복사한 뒤 복사본에만 사용자·기간·보안 정보를 주입하여 `dist/`에 생성합니다. `vba-files`, `config.json`, 개발 원본은 임시 배포값으로 변경하지 않으므로 별도의 소스 복원 작업이 필요하지 않습니다. 저장된 파일에는 `사용안내` 시트만 보이며, 매크로를 허용해 열면 기간 확인 후 업무 시트가 표시됩니다.

자동 검증은 다음을 확인합니다.

1. 매크로 차단 상태에서 안내 시트만 표시되는지
2. 매크로 허용 상태에서 업무 시트가 열리는지
3. 정상 저장 후 화면이 복원되는지
4. 다시 연 파일이 안내 시트만 표시하는 잠금 상태인지

## 배포 대상 사용자 확인

정상 업무 화면에서는 배포 대상 사용자 이름을 표시하지 않습니다. 관리자가 사용 중인 배포본에서 확인할 때는 `Alt+F8`을 누르고 `배포정보확인`을 실행한 뒤 VBA 프로젝트 암호를 입력합니다. 암호가 맞으면 `배포정보` 시트에 사용자, 버전, 배포일, 만료일이 표시됩니다.

`배포정보` 시트는 다른 시트로 이동하면 자동으로 다시 `VeryHidden` 상태가 되며, 시트 내용도 같은 암호로 보호됩니다. VBA 프로젝트 잠금까지 완료된 배포본에서는 일반 사용자가 이 시트의 표시 상태를 바꿀 수 없습니다.

## VBA 소스 열람 잠금

Excel은 VBA 프로젝트 암호 설정을 자동화하는 공식 API를 제공하지 않으므로 배포본마다 마지막 한 단계는 수동으로 처리합니다.

1. 배포본을 열고 `Alt+F11`
2. `도구 > VBAProject 속성 > 보호`
3. `보기 위해 프로젝트 잠금` 체크
4. 빌드 결과에 표시된 `vba_project_password`를 두 번 입력
5. 저장하고 Excel을 완전히 닫은 뒤 다시 열어 잠금을 확인

개발 원본의 VBA 프로젝트는 잠그지 않습니다.

## 만료 기간 연장

당일 코드는 날짜와 `renewal_secret`을 섞어 생성하므로 코드 문자열에서 날짜를 바로 알아볼 수 없습니다.

```powershell
npm run release:code
```

특정 날짜의 코드를 확인할 때는 다음처럼 실행합니다.

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\release-security.ps1 -Action Code -Date 2026-07-29
```

배포본 사용자가 만료 화면에 그날의 코드를 입력하면 현재 날짜부터 `renewal_days`만큼 연장됩니다.

## 한계

이 방식은 강한 DRM이 아닙니다. VBA 프로젝트 암호와 VeryHidden 시트는 일반적인 열람·수정과 단순 재배포를 어렵게 하는 수준입니다. 전문 도구로 파일을 분석하거나 수정하는 공격까지 막지는 않습니다.
