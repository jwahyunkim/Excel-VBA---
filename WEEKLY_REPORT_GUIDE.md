# 주간보고 항목과 글머리 설정

`config_주간보고`의 **PPT에 항목 표시**가 항목의 출력 여부를 결정합니다.
N인 항목은 글머리를 지정해도 출력되지 않습니다. **글머리 방식**은 출력되는 항목 앞의 기호나 번호를 결정합니다.

| 항목 표시 | 글머리 방식 | 출력 예시 |
|---|---|---|
| N | 기호 | 항목 숨김 |
| Y | 기호 (`ㄷ`) | ㄷ 중분류 |
| Y | 번호 매기기 | 1. 중분류 |
| Y | 글머리 없음 | 중분류 |

중분류와 소분류를 표시하려면 **D2와 E2를 Y**로 설정합니다. 타입은 N, 대분류와 업무명은 Y, 업무 레벨은 N으로 두면 다음과 같이 표시됩니다.

```text
대분류
    ㄷ 중분류
        ㄹ 소분류
            ㅁ 상위 업무
                하위 업무
                    세부 업무
```

업무 레벨의 Y/N은 `Level 1` 같은 이름표를 붙일지 결정합니다. N이어도 하위 업무명은 표시됩니다. 글머리 없음도 단계별 들여쓰기는 유지합니다.

G8:G14의 **PPT 표시 예시**는 설정 변경 즉시 갱신됩니다. 번호는 첫 항목의 예시입니다. 기존 `표시 안 함` 값은 `글머리 없음`과 같은 뜻으로 읽으며 설정 화면 갱신 시 새 이름으로 바뀝니다.

별도로 사용하는 최신 통합문서에는 해당 파일을 백업한 뒤 `modHoliday`, `modWeeklyPptReport` 두 모듈의 변경을 반영하고 `EnsureWeeklyReportConfigSheet`를 실행합니다. 모듈 전체를 교체할 때는 그 파일에서 별도로 수정한 코드가 있는지 먼저 비교합니다. 기존 Y/N 값은 자동으로 바꾸지 않으므로 D2/E2는 Y로 설정해야 합니다.

회귀 검증은 `powershell -NoProfile -ExecutionPolicy Bypass -File tests/test-weekly-report.ps1`로 실행합니다. Excel과 PowerPoint 및 VBA 프로젝트 개체 모델 접근이 필요하며, 임시 통합문서 복사본에서 실행됩니다.
